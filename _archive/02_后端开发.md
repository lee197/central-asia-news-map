# 02 · 后端开发

**预计耗时**：1-2 天
**前置条件**：完成 01 准备工作

---

## 这一步要做什么

在你的 Vultr 服务器上：
1. 装好 Python 环境
2. 把后端代码上传上去
3. 跑通新闻抓取
4. 跑通翻译和定位
5. 启动 API 服务

完成后，你能用浏览器访问 `http://你的服务器IP:8000/api/news` 看到 JSON 数据。

---

## 步骤 1：连接服务器并初始化环境

### 1.1 SSH 连服务器

打开 iTerm2：
```bash
ssh root@你的服务器IP
```
输入密码（粘贴密码时不显示是正常的），按回车进入。

看到 `root@central-asia-news:~#` 就是成功了。

### 1.2 更新系统并装基础工具

把下面整段命令复制粘贴到 iTerm2 里，回车执行：

```bash
apt update && apt upgrade -y
apt install -y python3 python3-pip python3-venv git sqlite3 nano curl
```

这一步会跑 1-3 分钟，会看到很多滚动文字，正常。

🎯 **完成标志**：最后回到 `root@xxx:~#` 提示符没有报红色 ERROR。

### 1.3 创建项目目录

```bash
mkdir -p /opt/news-map
cd /opt/news-map
```

### 1.4 创建 Python 虚拟环境

```bash
python3 -m venv venv
source venv/bin/activate
```

执行完第二条后，提示符变成 `(venv) root@xxx:/opt/news-map#`，前面多了 `(venv)`，说明虚拟环境激活成功。

⚠️ **注意**：每次重新 SSH 连服务器都要先 `cd /opt/news-map && source venv/bin/activate`，否则后面的命令会找不到 Python 包。

### 1.5 安装 Python 依赖

```bash
pip install fastapi uvicorn[standard] httpx anthropic feedparser python-dateutil apscheduler
```

跑 1 分钟左右。

🎯 **完成标志**：最后一行显示 `Successfully installed ...`。

---

## 步骤 2：写后端代码

### 2.1 创建配置文件

在服务器上：
```bash
nano /opt/news-map/.env
```

会进入一个全屏编辑器。粘贴下面内容（**记得替换为你自己的 key**）：

```
ANTHROPIC_API_KEY=sk-ant-你的真实key
```

保存退出：
- 按 `Ctrl + O` 然后回车（保存）
- 按 `Ctrl + X`（退出）

### 2.2 创建主代码文件 `app.py`

```bash
nano /opt/news-map/app.py
```

粘贴下面所有内容：

```python
"""
中亚新闻地图 - 后端服务
功能：定时抓取 GDELT + RSS 新闻，调用 Claude 翻译并抽取地点，提供 API
"""
import os
import json
import sqlite3
import hashlib
import asyncio
import logging
from contextlib import asynccontextmanager
from datetime import datetime, timedelta, timezone

import httpx
import feedparser
from anthropic import Anthropic
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

logging.basicConfig(level=logging.INFO, format='%(asctime)s [%(levelname)s] %(message)s')
log = logging.getLogger(__name__)

DB_PATH = "/opt/news-map/news.db"
ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY")
if not ANTHROPIC_API_KEY:
    raise RuntimeError("ANTHROPIC_API_KEY 未设置，请检查 .env 文件")

claude = Anthropic(api_key=ANTHROPIC_API_KEY)

RSS_FEEDS = [
    {"url": "https://tengrinews.kz/rss/", "country": "KZ", "source": "Tengrinews"},
    {"url": "https://www.gazeta.uz/ru/rss/", "country": "UZ", "source": "Gazeta.uz"},
    {"url": "https://24.kg/rss/", "country": "KG", "source": "24.kg"},
    {"url": "https://asiaplustj.info/ru/rss.xml", "country": "TJ", "source": "Asia-Plus"},
    {"url": "https://eurasianet.org/rss.xml", "country": None, "source": "Eurasianet"},
]

CENTRAL_ASIA_BOUNDS = {"lat_min": 35, "lat_max": 56, "lng_min": 46, "lng_max": 87}

def init_db():
    conn = sqlite3.connect(DB_PATH)
    conn.execute("""
        CREATE TABLE IF NOT EXISTS news (
            id TEXT PRIMARY KEY,
            source_url TEXT UNIQUE NOT NULL,
            source_name TEXT,
            country TEXT,
            city TEXT,
            lat REAL,
            lng REAL,
            title_orig TEXT,
            title_zh TEXT,
            summary_orig TEXT,
            summary_zh TEXT,
            published_at TEXT,
            fetched_at TEXT DEFAULT CURRENT_TIMESTAMP,
            confidence REAL,
            lang_orig TEXT
        )
    """)
    conn.execute("CREATE INDEX IF NOT EXISTS idx_published ON news(published_at)")
    conn.execute("CREATE INDEX IF NOT EXISTS idx_country ON news(country)")
    conn.commit()
    conn.close()
    log.info("数据库初始化完成")

def url_to_id(url: str) -> str:
    return hashlib.sha1(url.encode()).hexdigest()[:12]

async def fetch_rss(feed_info: dict) -> list:
    """抓取单个 RSS 源"""
    items = []
    try:
        async with httpx.AsyncClient(timeout=20.0, follow_redirects=True) as client:
            resp = await client.get(feed_info["url"], headers={"User-Agent": "Mozilla/5.0"})
            parsed = feedparser.parse(resp.text)
            for entry in parsed.entries[:30]:
                items.append({
                    "url": entry.get("link", ""),
                    "title": entry.get("title", ""),
                    "summary": entry.get("summary", "")[:1500],
                    "source": feed_info["source"],
                    "country_hint": feed_info["country"],
                    "published": entry.get("published", "") or entry.get("updated", "")
                })
        log.info(f"RSS {feed_info['source']}: 拿到 {len(items)} 条")
    except Exception as e:
        log.error(f"RSS {feed_info['source']} 失败: {e}")
    return items

async def fetch_gdelt() -> list:
    """从 GDELT 抓中亚相关新闻"""
    items = []
    countries = ["KZ", "UZ", "KG", "TJ", "TM"]
    for cc in countries:
        try:
            url = f"https://api.gdeltproject.org/api/v2/doc/doc?query=sourcecountry:{cc}&mode=ArtList&maxrecords=20&format=json&sort=DateDesc"
            async with httpx.AsyncClient(timeout=20.0) as client:
                resp = await client.get(url)
                data = resp.json()
                for art in data.get("articles", []):
                    items.append({
                        "url": art.get("url", ""),
                        "title": art.get("title", ""),
                        "summary": art.get("title", ""),
                        "source": art.get("domain", "GDELT"),
                        "country_hint": cc,
                        "published": art.get("seendate", "")
                    })
            log.info(f"GDELT {cc}: 拿到数据")
        except Exception as e:
            log.error(f"GDELT {cc} 失败: {e}")
    return items

def already_have(url: str) -> bool:
    conn = sqlite3.connect(DB_PATH)
    row = conn.execute("SELECT 1 FROM news WHERE source_url = ?", (url,)).fetchone()
    conn.close()
    return row is not None

def llm_process(item: dict) -> dict | None:
    """调用 Claude：翻译 + 抽地点"""
    prompt = f"""你是新闻分析助手。给定一条新闻的原文标题和摘要，请：
1. 把标题和摘要翻译成简洁的中文
2. 识别新闻"主要发生地"的城市和国家（必须是中亚五国：哈萨克斯坦KZ/乌兹别克斯坦UZ/吉尔吉斯斯坦KG/塔吉克斯坦TJ/土库曼斯坦TM）
3. 给出该城市的经纬度（标准 WGS84）
4. 评估你的判断置信度 0-1

新闻原文：
标题：{item['title']}
摘要：{item['summary']}
来源国提示：{item.get('country_hint') or '未知'}

只返回 JSON，不要其他文字：
{{
  "title_zh": "中文标题",
  "summary_zh": "中文摘要 100-200 字",
  "country": "KZ/UZ/KG/TJ/TM 之一，如果不属于中亚返回 null",
  "city": "城市中文名",
  "lat": 41.31,
  "lng": 69.24,
  "confidence": 0.85,
  "lang_orig": "原文语种代码 ru/en/uz/kk 等"
}}"""
    try:
        msg = claude.messages.create(
            model="claude-haiku-4-5-20251001",
            max_tokens=600,
            messages=[{"role": "user", "content": prompt}]
        )
        text = msg.content[0].text.strip()
        if text.startswith("```"):
            text = text.split("```")[1]
            if text.startswith("json"):
                text = text[4:]
        data = json.loads(text.strip())
        if not data.get("country") or data["country"] not in ["KZ","UZ","KG","TJ","TM"]:
            return None
        if data.get("confidence", 0) < 0.5:
            return None
        b = CENTRAL_ASIA_BOUNDS
        if not (b["lat_min"] <= data["lat"] <= b["lat_max"] and b["lng_min"] <= data["lng"] <= b["lng_max"]):
            return None
        return data
    except Exception as e:
        log.error(f"LLM 处理失败: {e}")
        return None

def save_news(item: dict, llm_result: dict):
    conn = sqlite3.connect(DB_PATH)
    try:
        conn.execute("""
            INSERT OR IGNORE INTO news
            (id, source_url, source_name, country, city, lat, lng,
             title_orig, title_zh, summary_orig, summary_zh,
             published_at, confidence, lang_orig)
            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
        """, (
            url_to_id(item["url"]),
            item["url"],
            item["source"],
            llm_result["country"],
            llm_result["city"],
            llm_result["lat"],
            llm_result["lng"],
            item["title"],
            llm_result["title_zh"],
            item["summary"],
            llm_result["summary_zh"],
            item["published"],
            llm_result["confidence"],
            llm_result.get("lang_orig", "")
        ))
        conn.commit()
    finally:
        conn.close()

async def run_ingest():
    """完整抓取流程"""
    log.info("=== 开始抓取 ===")
    all_items = []
    for feed in RSS_FEEDS:
        all_items.extend(await fetch_rss(feed))
    all_items.extend(await fetch_gdelt())

    seen_urls = set()
    new_items = []
    for it in all_items:
        if not it["url"] or it["url"] in seen_urls or already_have(it["url"]):
            continue
        seen_urls.add(it["url"])
        new_items.append(it)

    log.info(f"去重后 {len(new_items)} 条新条目，开始 LLM 处理")

    processed = 0
    for it in new_items[:50]:
        result = llm_process(it)
        if result:
            save_news(it, result)
            processed += 1
        await asyncio.sleep(0.3)

    log.info(f"=== 抓取完成，新增 {processed} 条 ===")

def cleanup_old():
    cutoff = (datetime.now(timezone.utc) - timedelta(days=30)).isoformat()
    conn = sqlite3.connect(DB_PATH)
    conn.execute("DELETE FROM news WHERE fetched_at < ?", (cutoff,))
    conn.commit()
    conn.close()
    log.info("清理 30 天前数据完成")

@asynccontextmanager
async def lifespan(app: FastAPI):
    init_db()
    scheduler = AsyncIOScheduler()
    scheduler.add_job(run_ingest, "interval", minutes=30, next_run_time=datetime.now())
    scheduler.add_job(cleanup_old, "cron", hour=3)
    scheduler.start()
    log.info("调度器启动：每 30 分钟抓取一次")
    yield
    scheduler.shutdown()

app = FastAPI(title="中亚新闻地图 API", lifespan=lifespan)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["GET"],
    allow_headers=["*"],
)

@app.get("/api/news")
def get_news(days: int = 7, country: str | None = None, limit: int = 500):
    cutoff = (datetime.now(timezone.utc) - timedelta(days=days)).isoformat()
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    sql = "SELECT * FROM news WHERE published_at > ?"
    params = [cutoff]
    if country and country != "ALL":
        sql += " AND country = ?"
        params.append(country)
    sql += " ORDER BY published_at DESC LIMIT ?"
    params.append(limit)
    rows = conn.execute(sql, params).fetchall()
    conn.close()
    items = [dict(row) for row in rows]
    return {"count": len(items), "items": items}

@app.get("/api/health")
def health():
    conn = sqlite3.connect(DB_PATH)
    total = conn.execute("SELECT COUNT(*) FROM news").fetchone()[0]
    last = conn.execute("SELECT MAX(fetched_at) FROM news").fetchone()[0]
    conn.close()
    return {"status": "ok", "total_news": total, "last_fetched": last}

@app.get("/")
def root():
    return {"name": "中亚新闻地图 API", "endpoints": ["/api/news", "/api/health"]}
```

保存退出（`Ctrl+O` 回车，`Ctrl+X`）。

### 2.3 创建启动脚本

```bash
nano /opt/news-map/start.sh
```

粘贴：

```bash
#!/bin/bash
cd /opt/news-map
source venv/bin/activate
export $(cat .env | xargs)
exec uvicorn app:app --host 0.0.0.0 --port 8000
```

保存退出。然后给执行权限：

```bash
chmod +x /opt/news-map/start.sh
```

---

## 步骤 3：第一次启动测试

### 3.1 手动启动

```bash
cd /opt/news-map
source venv/bin/activate
export $(cat .env | xargs)
uvicorn app:app --host 0.0.0.0 --port 8000
```

应该看到：
```
INFO: Started server process [...]
INFO: Application startup complete.
INFO: Uvicorn running on http://0.0.0.0:8000
2026-04-30 ... [INFO] 数据库初始化完成
2026-04-30 ... [INFO] 调度器启动：每 30 分钟抓取一次
2026-04-30 ... [INFO] === 开始抓取 ===
```

第一次抓取会跑 5-15 分钟（要调 50 次 LLM）。

### 3.2 浏览器测试

在你**本地电脑**（不是服务器）的浏览器里，访问：

```
http://你的服务器IP:8000/api/health
```

应该看到 JSON：
```json
{"status":"ok","total_news":0,"last_fetched":null}
```

⚠️ **如果浏览器打不开**：
- 检查 VPN 是否开着
- 检查服务器防火墙：在服务器上跑 `ufw status`，如果是 active 就跑 `ufw allow 8000`

### 3.3 等待第一次抓取完成

回到 SSH 终端看日志，等到看到：
```
=== 抓取完成，新增 XX 条 ===
```

然后浏览器再访问：
```
http://你的服务器IP:8000/api/news
```

应该看到 JSON 数据，包含 `count` 和 `items` 数组。

🎯 **完成标志**：能在浏览器看到包含真实新闻的 JSON。

### 3.4 停止服务

回到 SSH 终端，按 `Ctrl + C` 停止。

---

## 步骤 4：配置成系统服务（自动启动）

手动启动只是测试。我们要让服务器开机自动启动，断开 SSH 也继续运行。

### 4.1 创建 systemd 服务

```bash
nano /etc/systemd/system/news-map.service
```

粘贴：

```ini
[Unit]
Description=Central Asia News Map API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/news-map
ExecStart=/opt/news-map/start.sh
Restart=always
RestartSec=10
StandardOutput=append:/var/log/news-map.log
StandardError=append:/var/log/news-map.log

[Install]
WantedBy=multi-user.target
```

保存退出。

### 4.2 启用并启动

```bash
systemctl daemon-reload
systemctl enable news-map
systemctl start news-map
systemctl status news-map
```

最后一条命令应该看到 `Active: active (running)`。按 `q` 退出状态查看。

### 4.3 看日志

```bash
tail -f /var/log/news-map.log
```

按 `Ctrl + C` 退出查看。

🎯 **完成标志**：
- `systemctl status news-map` 显示 active (running)
- 浏览器访问 `http://你的服务器IP:8000/api/news` 还能看到 JSON

---

## 步骤 5：开放防火墙端口

```bash
ufw allow 22
ufw allow 8000
ufw --force enable
ufw status
```

应该看到 22 和 8000 都是 ALLOW。

---

## 全部完成检查表

- [ ] 服务器装好 Python 环境
- [ ] `/opt/news-map/app.py` 文件存在
- [ ] `/opt/news-map/.env` 包含真实的 Claude API key
- [ ] `systemctl status news-map` 显示 running
- [ ] 浏览器访问 `http://你的IP:8000/api/news` 能看到至少几条新闻
- [ ] 防火墙 8000 端口开着

---

## 下一步

✅ 全部打勾 → 看 [`03_前端部署.md`](./03_前端部署.md)。

❓ 卡住了把错误日志发我：
```bash
tail -50 /var/log/news-map.log
```
把输出原样复制给我。
